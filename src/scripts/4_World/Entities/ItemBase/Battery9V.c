modded class Battery9V
{
	/*override void SetWet(float value, bool allow_client = false)
	{
		super.SetWet(value, allow_client);
		
		if (GetWet() > 0.05)
		{
			if (GetCompEM().GetEnergy0To1() > 0)
			{
				GetCompEM().SetEnergy(0);
			}
			
			if (!IsRuined())
			{
				SetHealth("", "", 0);
			}
		}
	}*/
};